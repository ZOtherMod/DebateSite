services:
  - type: web
    name: debate-platform
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && python3 app.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.6
